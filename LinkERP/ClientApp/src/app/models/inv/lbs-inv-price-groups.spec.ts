import { LBSINVPriceGroups } from './lbs-inv-price-groups';

describe('LBSINVPriceGroups', () => {
  it('should create an instance', () => {
    expect(new LBSINVPriceGroups()).toBeTruthy();
  });
});
