import { LbsInvWarehouseNextNumbers } from './lbs-inv-warehouse-next-numbers';

describe('LbsInvWarehouseNextNumbers', () => {
  it('should create an instance', () => {
    expect(new LbsInvWarehouseNextNumbers()).toBeTruthy();
  });
});
