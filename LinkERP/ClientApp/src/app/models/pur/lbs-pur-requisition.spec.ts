import { LBSPURRequisition } from './lbs-pur-requisition';

describe('LBSPURRequisition', () => {
  it('should create an instance', () => {
    expect(new LBSPURRequisition()).toBeTruthy();
  });
});
