import { LBSPURLandedCostShipmentBookingProductStyleMatrix } from './lbs-pur-landed-cost-shipment-booking-product-style-matrix';

describe('LBSPURLandedCostShipmentBookingProductStyleMatrix', () => {
  it('should create an instance', () => {
    expect(new LBSPURLandedCostShipmentBookingProductStyleMatrix()).toBeTruthy();
  });
});
